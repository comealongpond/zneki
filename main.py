import arcade

# Set constants for the screen size
SCREEN_WIDTH = 1920
SCREEN_HEIGHT = 1080

arcade.open_window(SCREEN_WIDTH, SCREEN_HEIGHT, "Zneki")

arcade.start_render()





arcade.finish_render()

# Keep the window open until the user hits the 'close' button
arcade.run()